# Server
PORT=3333
NODE_ENV=development

# JWT
JWT_SECRET=sua-chave-secreta-aqui

# Database (SQLite - nao precisa configurar)
# Se quiser usar PostgreSQL futuramente:
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname
